%h1 Listing posts

%table.zebra-striped
  %tr
    %th Title
    %th Content
    %th Published?
    %th Published at
    %th
    %th
    %th

  - @posts.each do |post|
    %tr
      %td= post.title
      %td= post.content
      %td= post.published
      %td= post.published_at.try(:to_s, :short)
      %td= link_to 'Show', post_path(post)
      %td= link_to 'Edit', edit_admin_post_path(post)
      %td= link_to 'Destroy', admin_post_path(post), :confirm => 'Are you sure?', :method => :delete

%br

= link_to 'New Post', new_admin_post_path, :class => "btn xlarge primary"
